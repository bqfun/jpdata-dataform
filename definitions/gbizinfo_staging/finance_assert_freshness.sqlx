config {
  type: "assertion",
  schema: "gbizinfo_staging",
  tags: ["daily"]
}

SELECT
  COUNT(*) AS num_rows,
  CURRENT_DATE("Asia/Tokyo") - MAX(fiscal_year_cover_page.end_date) AS delay,
FROM
  ${ref("gbizinfo_staging", "finance_stg")}
HAVING
  num_rows < 22526 -- 2022-11-13時点の行数
  OR INTERVAL 102 DAY < delay
