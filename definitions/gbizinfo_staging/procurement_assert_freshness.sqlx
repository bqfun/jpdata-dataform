config {
  type: "assertion",
  schema: "gbizinfo_staging",
  tags: ["daily", "gbizinfo"]
}

WITH freshness AS (
  SELECT
    CURRENT_DATE("Asia/Tokyo") - MAX(date_of_order) AS delay
  FROM
    ${ref("gbizinfo_staging", "procurement_stg")} )

SELECT
  *
FROM
  freshness
WHERE
  delay >= INTERVAL 98 DAY
