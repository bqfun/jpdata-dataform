config {
  type: "assertion",
  schema: "houjinbangou_staging",
  tags: ["monthly"]
}

SELECT
  CURRENT_DATE("Asia/Tokyo") - MAX(update_date) AS delay,
FROM
  ${ref("houjinbangou_staging", "latest_stg")}
HAVING
  INTERVAL 32 DAY < delay
